<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{TITLE}}</title>
  <style>
    body {
      font-family: sans-serif;
      text-align: center;
      padding: 10vh 2rem;
      background: #fafafa;
      line-height: 1.4;
    }
    h1 {
      font-size: 1.5rem;             /* scale header */
      margin-bottom: 1rem;
    }
    img {
      max-width: 80%;              /* scale image on mobile */
      height: auto;
      margin-bottom: 1.5rem;
    }
  </style>
</head>
<body>
  {{IMAGE}}
  <h1>{{TITLE}}</h1>
  <p>{{MESSAGE}}</p>
</body>
</html>